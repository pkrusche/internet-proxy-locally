# GENERATED FILE — do not edit.
#
# Rendered from config.toml + data/lab/fixtures.toml by `ipl-lab`,
# via data/templates/smokescreen.yaml.j2.
# To change the test policy, edit data/lab/fixtures.toml and run
# `ipl-lab policy`. A hand edit here is silently overwritten and is
# caught by the test suite.
#
# TEST POLICY — NEVER use for normal operation. config/smokescreen.yaml
# plus the `[policy.test]` fixtures from data/lab/fixtures.toml. See docs/lab.md.
#
# Activate with:  ipl-lab --engine smokescreen up
# Deactivate with `ipl up` (recreates with the real policy).

---
version: v1

services: []

default:
  name: default
  project: internet-proxy-locally
  action: enforce
  allowed_domains:
    - github.com
    - "*.github.com"
    - "*.githubusercontent.com"
    - pypi.org
    - files.pythonhosted.org
    - registry.npmjs.org
    # --- [policy.test] in data/lab/fixtures.toml: adversarial fixtures only ---
    - "*.nip.io"
    - "*.sslip.io"
    - public-only.fixture.test
    - mixed-public-first.fixture.test
    - mixed-private-first.fixture.test
    - "*.rebind.fixture.test"
