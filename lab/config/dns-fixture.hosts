# GENERATED FILE — do not edit.
# Automatically regenerated by `ipl-lab up` from config.toml and
# data/lab/fixtures.toml, via data/templates/dns-fixture.hosts.j2.
# Edit the source files; manual changes here are overwritten on startup.
#
# TEST FIXTURE — served by the dnsmasq container that `ipl-lab up`
# starts, and read only through it. Never present in normal operation.
#
# Source of truth: `[fixture.records]` in data/lab/fixtures.toml. Full rationale
# for what this fixture is for and why it has to be a local dnsmasq
# container rather than a bind-mounted `/etc/hosts`: docs/lab.md.
#
# `rebind.fixture.test` is NOT here: its answers change between the
# first lookup and the next, which no static file can express, so
# data/images/dnsfixture/rebind.py serves that zone instead.

# Control: one public address, nothing else — must be reachable through
# the proxy or the mixed-answer rows below cannot be attributed.
9.9.9.9         public-only.fixture.test

# Mixed answers, both orderings (docs/findings.md, "Mixed DNS answers").
#
# Public answer first, private second.
9.9.9.9         mixed-public-first.fixture.test
10.0.0.1        mixed-public-first.fixture.test

# Private answer first, public second.
10.0.0.1        mixed-private-first.fixture.test
9.9.9.9         mixed-private-first.fixture.test
