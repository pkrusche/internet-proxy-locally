# project-sandbox — do not commit agent secrets
.project-sandbox/
.devcontainer/
__pycache__/
*.pyc
.venv/

# results/ is deliberately NOT ignored: docs/comparison.md is generated
# from those files by scripts/report.py, so a checkout without them
# cannot re-derive or check the published comparison — only believe it.
