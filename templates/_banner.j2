# GENERATED FILE — do not edit.
#
{% if test_policy %}
# Rendered from config.toml + lab/fixtures.toml by ./lab.py,
# via {{ template_name }}.
# To change the test policy, edit lab/fixtures.toml and run
# `./lab.py policy`. A hand edit here is silently overwritten and is
# caught by the test suite.
{% else %}
# Rendered from config.toml by ./run.py, via {{ template_name }}.
# To change the allowlist, edit config.toml and run `./run.py policy`
# (or just `./run.py up`, which regenerates first). A hand edit here is
# silently overwritten and is caught by the test suite.
{% endif %}
#
