"""config.toml in, engine configs out.

`config` parses and validates the source policy; `render` turns it into
each engine's config through the shared templates.
"""

from __future__ import annotations
