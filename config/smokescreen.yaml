# GENERATED FILE — do not edit.
#
# Rendered from config.toml by `ipl`, via data/templates/smokescreen.yaml.j2.
# To change the allowlist, edit config.toml and run `ipl policy`
# (or just `ipl up`, which regenerates first). A hand edit here is
# silently overwritten and is caught by the test suite.
#
# Smokescreen egress ACL: default deny, enforce mode. This is the
# Smokescreen rendering of the shared logical policy in docs/policy.md.
#
# Requests carry no role (no client TLS cert in v1), so every request
# falls through to the `default` service below — see
# `allow_missing_role: true` in config/smokescreen.conf.yaml. Private/
# loopback/link-local blocking is built into Smokescreen and is NOT
# disabled here (never pass --unsafe-allow-private-ranges).

---
version: v1

services: []

default:
  name: default
  project: internet-proxy-locally
  action: enforce
  allowed_domains:
    - api.anthropic.com
    - claude.ai
    - code.claude.com
    - platform.claude.com
    - api.openai.com
    - auth.openai.com
    - chatgpt.com
    - github.com
    - "*.github.com"
    - "*.githubusercontent.com"
    - pypi.org
    - files.pythonhosted.org
    - registry.npmjs.org
